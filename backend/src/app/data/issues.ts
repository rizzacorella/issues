import { Issue } from '../models/issue.js'

const issues: Issue[] = [
  {
    id: 1,
    title: 'No Error Logs',
    description: 'Errors are not logged when they are encountered.'
  }
]

export default issues
